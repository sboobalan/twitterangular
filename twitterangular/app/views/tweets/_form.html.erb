<%= form_with(model: tweet, local: true) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, id: :tweet_username %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text, id: :tweet_text %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :tweet_status %>
  </div>

  <div class="field">
    <%= form.label :approvedby %>
    <%= form.text_field :approvedby, id: :tweet_approvedby %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :tweet_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
